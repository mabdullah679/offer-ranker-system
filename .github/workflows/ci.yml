name: ci

on:
  push:
    branches: ["dev"]
  workflow_dispatch:

jobs:
  build-test-scan:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: "1"
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up venv and install dependencies
        run: |
          python -m venv .venv
          source .venv/bin/activate
          python -m pip install --upgrade pip
          pip install -r api/requirements.txt
          pip install -r training/requirements.txt
          pip install pip-audit==2.7.3

      - name: Run pytest
        run: .venv/bin/pytest api/tests

      - name: Run data quality checks
        run: .venv/bin/python dq/dq_cli.py validate training/data/customers.csv --config dq/dq_rules.yaml --format json

      - name: Dependency audit
        run: |
          .venv/bin/pip-audit -r api/requirements.txt -r training/requirements.txt --severity HIGH --severity CRITICAL

      - name: Build Docker image (no push)
        run: docker build -t offer-ranker-api:ci ./api

      - name: Trivy image scan
        uses: aquasecurity/trivy-action@0.24.0
        with:
          image-ref: "offer-ranker-api:ci"
          severity: "CRITICAL,HIGH"
          ignore-unfixed: true
          vuln-type: "os,library"
          exit-code: "1"
